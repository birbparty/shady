; Generated by Shady toGeoPica (PICA200 geometry shader)
; from subdivideGeo

.gsh point c0
.fvec projection[4]
.out outpos position
.out outclr color
.constf shady_c0(0.5,0.5,0.5,0.5)

.entry gmain
.proc gmain
	mov r0, v2
	add r1, v0, r0
	mul r0, shady_c0, r1
	mov r1, r0
	mov r0, v4
	add r2, v2, r0
	mul r0, shady_c0, r2
	mov r2, r0
	mov r0, v0
	add r3, v4, r0
	mul r0, shady_c0, r3
	mov r3, r0
	setemit 0
	dp4 outpos.x, projection[0], v0
	dp4 outpos.y, projection[1], v0
	dp4 outpos.z, projection[2], v0
	dp4 outpos.w, projection[3], v0
	mov outclr, v1
	emit
	setemit 1
	dp4 outpos.x, projection[0], r1
	dp4 outpos.y, projection[1], r1
	dp4 outpos.z, projection[2], r1
	dp4 outpos.w, projection[3], r1
	mov outclr, v3
	emit
	setemit 2, prim
	dp4 outpos.x, projection[0], r3
	dp4 outpos.y, projection[1], r3
	dp4 outpos.z, projection[2], r3
	dp4 outpos.w, projection[3], r3
	mov outclr, v5
	emit
	setemit 0
	dp4 outpos.x, projection[0], r1
	dp4 outpos.y, projection[1], r1
	dp4 outpos.z, projection[2], r1
	dp4 outpos.w, projection[3], r1
	mov outclr, v1
	emit
	setemit 1
	dp4 outpos.x, projection[0], v2
	dp4 outpos.y, projection[1], v2
	dp4 outpos.z, projection[2], v2
	dp4 outpos.w, projection[3], v2
	mov outclr, v3
	emit
	setemit 2, prim
	dp4 outpos.x, projection[0], r2
	dp4 outpos.y, projection[1], r2
	dp4 outpos.z, projection[2], r2
	dp4 outpos.w, projection[3], r2
	mov outclr, v5
	emit
	setemit 0
	dp4 outpos.x, projection[0], r3
	dp4 outpos.y, projection[1], r3
	dp4 outpos.z, projection[2], r3
	dp4 outpos.w, projection[3], r3
	mov outclr, v1
	emit
	setemit 1
	dp4 outpos.x, projection[0], r2
	dp4 outpos.y, projection[1], r2
	dp4 outpos.z, projection[2], r2
	dp4 outpos.w, projection[3], r2
	mov outclr, v3
	emit
	setemit 2, prim
	dp4 outpos.x, projection[0], v4
	dp4 outpos.y, projection[1], v4
	dp4 outpos.z, projection[2], v4
	dp4 outpos.w, projection[3], v4
	mov outclr, v5
	emit
	end
.end
